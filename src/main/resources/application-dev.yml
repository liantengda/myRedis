server:
  port: 8080

spring:
  mvc:
    throw-exception-if-no-handler-found: true
  resources:
    add-mappings: false
  redis:
    database: 0 # Redis数据库索引（默认为0）
    host: 127.0.0.1
    port: 6379
#    password: 12345
#showSql
logging:
  level:
    com:
      example:
        mapper: debug